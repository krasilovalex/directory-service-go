Скопируй этот код в свой README.md:
Markdown

# 🏢 Corporate Directory Service

![Go Version](https://img.shields.io/badge/Go-1.23-00ADD8?style=flat&logo=go)
![Gin Framework](https://img.shields.io/badge/Gin-Framework-00ADD8?style=flat&logo=go)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-336791?style=flat&logo=postgresql)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)
![Swagger](https://img.shields.io/badge/Swagger-API%20Docs-85EA2D?style=flat&logo=swagger)

**Directory Service** — это микросервис для управления корпоративным справочником сотрудников, отделов и офисов.

Проект разработан с упором на принципы **Clean Architecture**, что обеспечивает легкость поддержки, тестирования и масштабирования. Включает в себя REST API, автоматическую документацию Swagger и встроенную панель администратора.

---

## ⚡ Особенности

* 🏗 **Clean Architecture:** Четкое разделение слоев (Delivery -> UseCase -> Repository).
* 🔌 **RESTful API:** Полный CRUD для сущностей (Departments, Locations, Positions).
* 🐘 **PostgreSQL + pgx:** Производительная работа с базой данных через пул соединений.
* 📄 **Swagger UI:** Автогенерируемая документация API.
* 💻 **Admin Panel:** Веб-интерфейс для управления данными (Vanilla JS + HTML).
* 🐳 **Docker:** Быстрый запуск окружения (БД).
* ⚙️ **Config:** Управление конфигурацией через `.env`.

---

## 🛠 Технический стек

| Категория | Технология | Описание |
|---|---|---|
| **Language** | Go (Golang) | Основной язык разработки |
| **Framework** | Gin Gonic | Быстрый HTTP веб-фреймворк |
| **Database** | PostgreSQL | Реляционная СУБД |
| **Driver** | pgx/v5 | Высокопроизводительный драйвер и toolkit для Postgres |
| **Docs** | Swaggo | Генератор Swagger-документации из комментариев кода |
| **Config** | Godotenv | Загрузка переменных окружения из .env |

---

## 📂 Структура проекта

Проект следует стандартной структуре Go-проектов (Go Standard Project Layout):

```text
directory-service/
├── cmd/
│   └── server/
│       └── main.go       # Точка входа в приложение
├── internal/
│   ├── delivery/         # HTTP Handlers (слой доставки)
│   ├── usecase/          # Business Logic (бизнес-логика)
│   ├── repository/       # Database access (работа с БД)
│   └── domain/           # Models & Entities (основные структуры)
├── web/                  # Frontend (HTML/CSS/JS)
├── docs/                 # Swagger файлы
├── docker-compose.yaml   # Инфраструктура (БД)
├── .env                  # Конфигурация (в gitignore)
└── go.mod                # Зависимости

```


## 🚀 Быстрый старт
Предварительные требования
Go 1.20+

Docker & Docker Compose

1. Клонирование репозитория

```bash
git clone [https://github.com/krasiloalex/directory-service.git](https://github.com/krasilovalex/directory-service.git)
```
```bash
cd directory-service
```

2. Настройка окружения
Создайте файл .env в корне проекта:

Фрагмент кода
```bash
DB_URL=postgres://user:pass@localhost:5436/directory_db
```

3. Запуск Базы Данных
Поднимаем PostgreSQL в контейнере:


```bash
docker-compose up -d
```

4. Запуск приложения


```bash
go run cmd/server/main.go
```
Сервер запустится на порту :8080.

## 📖 Документация и Интерфейсы
Swagger UI
Полная документация API доступна по адресу: http://localhost:8080/swagger/index.html

Панель Администратора
Веб-интерфейс для управления данными: http://localhost:8080

📡 Примеры API
Получить все отделы: GET /departments

Создать новый офис: POST /locations

```json

{
  "city": "Moscow",
  "address": "Red Square, 1"
}
```
Обновить должность: PUT /positions/{id}


```json
{
  "title": "Senior Go Developer"
}
```

## 📝 Лицензия
Этот проект распространяется под лицензией MIT.